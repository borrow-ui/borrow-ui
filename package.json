{
  "name": "borrow-ui",
  "repository": "git@github.com:borrow-ui/borrow-ui.git",
  "author": "Vittorio Zamboni <vittorio.zamboni@gmail.com>",
  "description": "A simple design system for React",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "ui:build": "cd packages/ui && yarn build",
    "website:build": "cd packages/website && yarn build"
  }
}
