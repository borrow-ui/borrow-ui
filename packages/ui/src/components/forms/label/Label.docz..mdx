---
name: Label
route: /forms/label
menu: Forms
---

import { Fragment } from 'react';
import { Playground, Props } from 'docz';

import { Label } from './Label';

# Label

`Label` component generates the label of a field.

The examples are explained in the `Field` documentation,
as all props are exposed by the `Field` component.

## Props

<Props of={Label} />
